import { ColumnContentDirective } from './column-content.directive';

describe('ColumnContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnContentDirective();
    expect(directive).toBeTruthy();
  });
});
