import { MultiSortDirective } from './multi-sort.directive';

describe('MultiSortDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiSortDirective();
    expect(directive).toBeTruthy();
  });
});
