import { RowFilterControlDirective } from './row-filter-control.directive';

describe('RowFilterControlDirective', () => {
  it('should create an instance', () => {
    const directive = new RowFilterControlDirective();
    expect(directive).toBeTruthy();
  });
});
