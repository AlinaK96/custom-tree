import { OutsideViewportOnlyDirective } from './outside-viewport-only.directive';

describe('OutsideViewportOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new OutsideViewportOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
